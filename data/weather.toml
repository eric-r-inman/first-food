# Weather.  A weather effect placed as a layer over the terrain.  Reuses the
# terrain shape (keyed glyph + color + props).  Graphics are placeholders ("?")
# until assigned in the weather editor.

[[weather]]
id = "Weather A"
key = "W"
glyph = "?"
color = "white"
