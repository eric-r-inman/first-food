# Inhabitant archetypes.  These are theme-specific content values; the engine
# treats them generically, so a different theme is just a different data set.

[[archetype]]
id = "forager"
name = "Forager"
max_satiation = 100
hunger_rate = 2

[[archetype]]
id = "fisher"
name = "Fisher"
max_satiation = 100
hunger_rate = 3
